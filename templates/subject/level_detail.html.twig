{% include 'partials/header.html.twig' %}

<h1>Exercices pour {{ subject.name }} - {{ level.name }}</h1>

{% if exercises is not empty %}
    <ul>
        {% for chapiter in chapiters %}
            <li>{{ chapiter.name }}</li>
        {% endfor %}
    </ul>
{% else %}
    <p>Aucun chapitre disponible pour cette matière et ce niveau.</p>
{% endif %}

{% include 'partials/footer.html.twig' %}